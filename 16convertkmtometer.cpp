#include<iostream>
using namespace std;
int main ()
{
    //convert meter into km.
    float km, meter;
    cout<<"Enter the meter to convert into km : ";
    cin>>meter;
    km=meter/1000;
    cout<<meter<<" meter "<<" into "<<km <<" km ";
    
}

